class Solution {
public:
    bool checkGoodInteger(int n) {
        long long digitSum = 0, squareSum = 0;

        while (n > 0) {
            int d = n % 10;
            digitSum += d;
            squareSum += 1LL * d * d;
            n /= 10;
        }

        return (squareSum - digitSum) >= 50;
    }
};
